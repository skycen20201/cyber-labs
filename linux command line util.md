cat- concatenate - The primary use is to read the contents of files and display them to the standard output (your screen)
grep- global regular expression print - This command is used to search for specific text strings or patterns (regular expressions) within files and prints only the lines that match the pattern
